<div class="poster_icon_wrapper">
	<%= link_to image_tag(season.show.poster), show_path(season.show), class: "poster_icon" %>
	<br>
	<%= link_to season.title, show_path(season.show) %>
	<br>
	season 
	<%= season.season_num %>
</div> 