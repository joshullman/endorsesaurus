<div id=<%="#{rec.info.medium_id}"%> class="recommendation_row_movie">
	<span class="rec_points"><%= rec.user_points %></span>
	<span class="upvote_icon"><%= render partial: 'layouts/upvote_icon', locals: { user: user, current_user_likes: current_user_likes, rec: rec, current_user: current_user } %></span>
	<span class="title_center"><%= link_to rec.info.title, movie_path(rec.info) %></span>
	<span class="icons_right"><%= render partial: 'layouts/rec_icons', locals: { user: user, current_user_likes: current_user_likes, rec: rec, current_user: current_user } %></span>
	<br>
	<br>
<!-- 	Recommended By:
	<br>
	<% rec.rec_by.each do |recommender| %>
		<div class="friend_icon_wrapper">
			<%= link_to image_tag('profile.jpg'), recommender, class: "photo_icon"%>
	    <br>
	    <%= link_to recommender.name, recommender, class: "text_center" %>
	    <br>
    </div>
	<% end %> -->
</div>