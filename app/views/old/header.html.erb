<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

<header class ="navbar-wrapper">
  <div class="site-name">
    <%= image_tag('derpy_dinosaur.png', size: "30") %>
    Endorsesaurus: Tip the Scales
  </div>
  <div class ="navbar-class">
    <% if current_user %>
      <ul class="navbar-left navbar">
        <li class="nav_element"><%= link_to "Movies", movies_path %></li>
        <li class="nav_element"><%= link_to "Shows", shows_path %></li>
        <li class="nav_element"><%= link_to "Tags", tags_path %></li>
      </ul>
      <ul class="search_bar navbar">
        <li class="search">
          <%= form_tag('/media/search', method: :get) do |f| %>
            <%= text_field_tag :title, params[:title] %>
            <%= select_tag "media", options_for_select(["Movies", "Shows"], "Shows")%>
            <%= submit_tag "Search" %>
          <% end %>
        </li>
      </ul>
      <ul class="navbar-right navbar">
        (<%= current_user.points %>)
        <%= link_to image_tag('profile.jpg'), user_path(current_user), class: "photo_icon_small" %>
        <li class="nav_element"><%= link_to current_user.name, dashboard_user_path(current_user) %>
          <ul>
            <li class="dropdown_element"><%= link_to "Profile", current_user %></li>
            <li class="dropdown_element"><%= link_to "Friends", friends_user_path(current_user) %></li>
            <li class="dropdown_element"><%= link_to "Movies", movies_user_path(current_user) %></li>
            <li class="dropdown_element"><%= link_to "Shows", shows_user_path(current_user) %></li>
            <li class="dropdown_element"><%= render 'devise/menu/registration_items' %></li>
            <li class="dropdown_element"><%= render 'devise/menu/login_items' %></li>
          </ul>
        </li>
      </ul>
    <% end %>
  </div>
</header>