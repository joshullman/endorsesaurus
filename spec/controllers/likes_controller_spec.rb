require 'rails_helper'

RSpec.describe LikesController, type: :controller do

	describe "POST create" do

		it "does nothing if a like exists and has the same value"
		it "updates all likes if a like exists and it is a show"
		it "updates the like if the media type is an episode or movie"
		it "creates likes for every episode if it's a show and doesn't already exist"
		it "creates li"
	end

end